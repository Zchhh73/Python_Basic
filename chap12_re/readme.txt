正则表达式(在代码中常简写为regex 、regexp 、RE 或re)是预先定义好的一个“规则字符串”
通过这个“规则字符串”可以匹配、查找和替换那些符合“规则”的文本。
表达式实现这些功能会比较简单，而且效率很高，唯一的困难之处在于编写合适的正则表达式。

1. 元字符
    元字符（Metacharacters）是用来描述其他字符的特殊字符，它由基本元字符和普通字符构
成。基本元字符是构成元字符的组成要素。\w+ \.

2. 字符转义
    在正则表达式中有时也需要字符转义，需要在w字母前加上反斜杠"\"."\."、"\?"

